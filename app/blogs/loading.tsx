import Container from "../../components/common/container/Container";
import { FC } from "react";

const BlogsLoading: FC = () => {
  return (
    <Container className="flex min-h-screen items-center justify-center">
      Loading...
    </Container>
  );
};

export default BlogsLoading;
